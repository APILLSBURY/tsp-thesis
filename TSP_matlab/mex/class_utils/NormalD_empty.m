function nd = NormalD_empty(nd)
    nd.N = 0;
    nd.temp_N = 0;
    nd.total = zeros(size(nd.total));
    nd.total2 = zeros(size(nd.total2));
    nd.temp_total = zeros(size(nd.temp_total));
    nd.temp_total2 = zeros(size(nd.temp_total2));
end